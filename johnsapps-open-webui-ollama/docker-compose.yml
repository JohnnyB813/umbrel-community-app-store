version: "3.7"

services:

  app_proxy:
    environment:
      # The format here is: <app-id>_<docker-service-name>_1
      APP_HOST: johnsapps-open-webui-ollama_web_1
      APP_PORT: 8080
      PROXY_AUTH_ADD: "false"
    
  web:
    image: ghcr.io/open-webui/open-webui:ollama@sha256:d602810b919f2282cf8c4a4fb9da7391c47eeb7a60700e38fcdcb5c8805aff46
    volumes:
      - ${APP_DATA_DIR}/data/open-webui-ollama/ollama:/root/.ollama
      - ${APP_DATA_DIR}/data/open-webui-ollama/open-webui:/app/backend/data
    runtime: nvidia
    restart: always
    environment:
      NVIDIA_VISIBLE_DEVICES: "all"
      NVIDIA_DRIVER_CAPABILITIES: "compute,utility"
